import { LayoutDashboard, CheckSquare, Users, BarChart3, Settings, Sparkles } from "lucide-react";
import { NavLink } from "@/components/NavLink";
import {
  Sidebar,
  SidebarContent,
  SidebarFooter,
  SidebarGroup,
  SidebarGroupContent,
  SidebarHeader,
  SidebarMenu,
  SidebarMenuButton,
  SidebarMenuItem,
  useSidebar,
} from "@/components/ui/sidebar";

const items = [
  { title: "Dashboard", url: "/", icon: LayoutDashboard },
  { title: "My Tasks", url: "/my-tasks", icon: CheckSquare },
  { title: "Team Tasks", url: "/team-tasks", icon: Users },
  { title: "Reports", url: "/reports", icon: BarChart3 },
  { title: "Settings", url: "/settings", icon: Settings },
];

export function AppSidebar() {
  const { state } = useSidebar();
  const collapsed = state === "collapsed";

  return (
    <Sidebar collapsible="icon" className="border-r border-sidebar-border/50 bg-transparent">
      <SidebarHeader className="border-b border-sidebar-border/40">
        <div className="flex items-center gap-3 px-2 py-4">
          <div className="relative flex h-9 w-9 shrink-0 items-center justify-center rounded-xl bg-gradient-to-br from-primary to-[hsl(220_90%_60%)] glow-primary">
            <Sparkles className="h-4 w-4 text-primary-foreground" />
          </div>
          {!collapsed && (
            <div className="flex flex-col leading-tight">
              <span className="font-semibold text-sidebar-foreground tracking-tight">
                Nimbus
              </span>
              <span className="text-[10px] uppercase tracking-[0.2em] text-sidebar-foreground/50">
                Workspace
              </span>
            </div>
          )}
        </div>
      </SidebarHeader>

      <SidebarContent className="px-2 py-4">
        <SidebarGroup>
          <SidebarGroupContent>
            <SidebarMenu className="gap-1.5">
              {items.map((item) => (
                <SidebarMenuItem key={item.title}>
                  <SidebarMenuButton asChild tooltip={item.title} className="h-11 rounded-xl">
                    <NavLink
                      to={item.url}
                      end
                      className="group relative text-sidebar-foreground/80 hover:bg-sidebar-accent/40 hover:text-sidebar-accent-foreground transition-all"
                      activeClassName="!bg-sidebar-accent !text-sidebar-accent-foreground !shadow-[inset_0_1px_0_0_hsl(210_30%_100%/0.08)] before:absolute before:left-0 before:top-1/2 before:h-5 before:w-0.5 before:-translate-y-1/2 before:rounded-r-full before:bg-primary before:shadow-[0_0_12px_hsl(199_95%_65%/0.8)]"
                    >
                      <item.icon className="h-[18px] w-[18px]" />
                      {!collapsed && <span className="text-sm">{item.title}</span>}
                    </NavLink>
                  </SidebarMenuButton>
                </SidebarMenuItem>
              ))}
            </SidebarMenu>
          </SidebarGroupContent>
        </SidebarGroup>
      </SidebarContent>

      {!collapsed && (
        <SidebarFooter className="p-3">
          <div className="glass rounded-2xl p-4 text-center">
            <div className="mx-auto mb-2 h-8 w-8 rounded-full bg-gradient-to-br from-primary to-[hsl(220_90%_60%)] glow-primary" />
            <p className="text-xs font-medium text-sidebar-foreground">Upgrade to Pro</p>
            <p className="mt-0.5 text-[10px] text-sidebar-foreground/60">Unlock automations</p>
          </div>
        </SidebarFooter>
      )}
    </Sidebar>
  );
}
